import { PlacaAutomovelAntigaPipe } from './placa-automovel-antiga.pipe';

describe('PlacaAutomovelAntigaPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaAutomovelAntigaPipe();
    expect(pipe).toBeTruthy();
  });
});
