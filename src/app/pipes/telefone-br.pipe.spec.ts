import { TelefoneBRPipe } from './telefone-br.pipe';

describe('TelefoneBRPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneBRPipe();
    expect(pipe).toBeTruthy();
  });
});
